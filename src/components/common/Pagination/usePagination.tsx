import { useEffect, useState } from "react";

function usePagination(itmes_length:number, items_per_page:number, start_range:number, end_range:number, set_start_range: (newValue: number) => void, set_end_range: (newValue: number) => void){
  console.log(itmes_length)

    return {
     itmes_length
    }
}

export default usePagination